// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: transport/internet/finalmask/realm/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../typeRegistry";
import { Config as Config1 } from "../../tls/config";

export const protobufPackage = "xray.transport.internet.finalmask.realm";

export interface Config {
  $type: "xray.transport.internet.finalmask.realm.Config";
  scheme: string;
  host: string;
  port: string;
  token: string;
  ID: string;
  stunServers: string[];
  tlsConfig: Config1 | undefined;
}

function createBaseConfig(): Config {
  return {
    $type: "xray.transport.internet.finalmask.realm.Config",
    scheme: "",
    host: "",
    port: "",
    token: "",
    ID: "",
    stunServers: [],
    tlsConfig: undefined,
  };
}

export const Config: MessageFns<Config, "xray.transport.internet.finalmask.realm.Config"> = {
  $type: "xray.transport.internet.finalmask.realm.Config" as const,

  encode(message: Config, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scheme !== "") {
      writer.uint32(10).string(message.scheme);
    }
    if (message.host !== "") {
      writer.uint32(18).string(message.host);
    }
    if (message.port !== "") {
      writer.uint32(26).string(message.port);
    }
    if (message.token !== "") {
      writer.uint32(34).string(message.token);
    }
    if (message.ID !== "") {
      writer.uint32(42).string(message.ID);
    }
    for (const v of message.stunServers) {
      writer.uint32(50).string(v!);
    }
    if (message.tlsConfig !== undefined) {
      Config1.encode(message.tlsConfig, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Config {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.port = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.ID = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.stunServers.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.tlsConfig = Config1.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Config {
    return {
      $type: Config.$type,
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.String(object.port) : "",
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      ID: isSet(object.ID) ? globalThis.String(object.ID) : "",
      stunServers: globalThis.Array.isArray(object?.stunServers)
        ? object.stunServers.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.stun_servers)
        ? object.stun_servers.map((e: any) => globalThis.String(e))
        : [],
      tlsConfig: isSet(object.tlsConfig)
        ? Config1.fromJSON(object.tlsConfig)
        : isSet(object.tls_config)
        ? Config1.fromJSON(object.tls_config)
        : undefined,
    };
  },

  toJSON(message: Config): unknown {
    const obj: any = {};
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== "") {
      obj.port = message.port;
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.ID !== "") {
      obj.ID = message.ID;
    }
    if (message.stunServers?.length) {
      obj.stunServers = message.stunServers;
    }
    if (message.tlsConfig !== undefined) {
      obj.tlsConfig = Config1.toJSON(message.tlsConfig);
    }
    return obj;
  },

  create(base?: DeepPartial<Config>): Config {
    return Config.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Config>): Config {
    const message = createBaseConfig();
    message.scheme = object.scheme ?? "";
    message.host = object.host ?? "";
    message.port = object.port ?? "";
    message.token = object.token ?? "";
    message.ID = object.ID ?? "";
    message.stunServers = object.stunServers?.map((e) => e) || [];
    message.tlsConfig = (object.tlsConfig !== undefined && object.tlsConfig !== null)
      ? Config1.fromPartial(object.tlsConfig)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(Config.$type, Config);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               v6.33.4
// source: transport/internet/finalmask/xmc/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../typeRegistry";

export const protobufPackage = "xray.transport.internet.finalmask.xmc";

export interface Profile {
  $type: "xray.transport.internet.finalmask.xmc.Profile";
  /** Resolve the UUID from https://api.mojang.com/users/profiles/minecraft/{username}. */
  username: string;
  uuid: Uint8Array;
  /**
   * Copy the signed textures property returned by
   * https://sessionserver.mojang.com/session/minecraft/profile/{uuid}?unsigned=false.
   */
  texturesValue: string;
  texturesSignature: string;
}

export interface Config {
  $type: "xray.transport.internet.finalmask.xmc.Config";
  password: string;
  rsaPrivateKey: Uint8Array;
  rsaPublicKey: Uint8Array;
  hostname: string;
  profiles: Profile[];
}

function createBaseProfile(): Profile {
  return {
    $type: "xray.transport.internet.finalmask.xmc.Profile",
    username: "",
    uuid: new Uint8Array(0),
    texturesValue: "",
    texturesSignature: "",
  };
}

export const Profile: MessageFns<Profile, "xray.transport.internet.finalmask.xmc.Profile"> = {
  $type: "xray.transport.internet.finalmask.xmc.Profile" as const,

  encode(message: Profile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.uuid.length !== 0) {
      writer.uint32(18).bytes(message.uuid);
    }
    if (message.texturesValue !== "") {
      writer.uint32(26).string(message.texturesValue);
    }
    if (message.texturesSignature !== "") {
      writer.uint32(34).string(message.texturesSignature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Profile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseProfile();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.uuid = reader.bytes();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.texturesValue = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.texturesSignature = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Profile {
    return {
      $type: Profile.$type,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      uuid: isSet(object.uuid) ? bytesFromBase64(object.uuid) : new Uint8Array(0),
      texturesValue: isSet(object.texturesValue)
        ? globalThis.String(object.texturesValue)
        : isSet(object.textures_value)
        ? globalThis.String(object.textures_value)
        : "",
      texturesSignature: isSet(object.texturesSignature)
        ? globalThis.String(object.texturesSignature)
        : isSet(object.textures_signature)
        ? globalThis.String(object.textures_signature)
        : "",
    };
  },

  toJSON(message: Profile): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.uuid.length !== 0) {
      obj.uuid = base64FromBytes(message.uuid);
    }
    if (message.texturesValue !== "") {
      obj.texturesValue = message.texturesValue;
    }
    if (message.texturesSignature !== "") {
      obj.texturesSignature = message.texturesSignature;
    }
    return obj;
  },

  create(base?: DeepPartial<Profile>): Profile {
    return Profile.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Profile>): Profile {
    const message = createBaseProfile();
    message.username = object.username ?? "";
    message.uuid = object.uuid ?? new Uint8Array(0);
    message.texturesValue = object.texturesValue ?? "";
    message.texturesSignature = object.texturesSignature ?? "";
    return message;
  },
};

messageTypeRegistry.set(Profile.$type, Profile);

function createBaseConfig(): Config {
  return {
    $type: "xray.transport.internet.finalmask.xmc.Config",
    password: "",
    rsaPrivateKey: new Uint8Array(0),
    rsaPublicKey: new Uint8Array(0),
    hostname: "",
    profiles: [],
  };
}

export const Config: MessageFns<Config, "xray.transport.internet.finalmask.xmc.Config"> = {
  $type: "xray.transport.internet.finalmask.xmc.Config" as const,

  encode(message: Config, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.password !== "") {
      writer.uint32(10).string(message.password);
    }
    if (message.rsaPrivateKey.length !== 0) {
      writer.uint32(66).bytes(message.rsaPrivateKey);
    }
    if (message.rsaPublicKey.length !== 0) {
      writer.uint32(74).bytes(message.rsaPublicKey);
    }
    if (message.hostname !== "") {
      writer.uint32(82).string(message.hostname);
    }
    for (const v of message.profiles) {
      Profile.encode(v!, writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Config {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConfig();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.password = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.rsaPrivateKey = reader.bytes();
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.rsaPublicKey = reader.bytes();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.hostname = reader.string();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.profiles.push(Profile.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Config {
    return {
      $type: Config.$type,
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      rsaPrivateKey: isSet(object.rsaPrivateKey)
        ? bytesFromBase64(object.rsaPrivateKey)
        : isSet(object.rsa_private_key)
        ? bytesFromBase64(object.rsa_private_key)
        : new Uint8Array(0),
      rsaPublicKey: isSet(object.rsaPublicKey)
        ? bytesFromBase64(object.rsaPublicKey)
        : isSet(object.rsa_public_key)
        ? bytesFromBase64(object.rsa_public_key)
        : new Uint8Array(0),
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
      profiles: globalThis.Array.isArray(object?.profiles) ? object.profiles.map((e: any) => Profile.fromJSON(e)) : [],
    };
  },

  toJSON(message: Config): unknown {
    const obj: any = {};
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.rsaPrivateKey.length !== 0) {
      obj.rsaPrivateKey = base64FromBytes(message.rsaPrivateKey);
    }
    if (message.rsaPublicKey.length !== 0) {
      obj.rsaPublicKey = base64FromBytes(message.rsaPublicKey);
    }
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => Profile.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Config>): Config {
    return Config.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Config>): Config {
    const message = createBaseConfig();
    message.password = object.password ?? "";
    message.rsaPrivateKey = object.rsaPrivateKey ?? new Uint8Array(0);
    message.rsaPublicKey = object.rsaPublicKey ?? new Uint8Array(0);
    message.hostname = object.hostname ?? "";
    message.profiles = object.profiles?.map((e) => Profile.fromPartial(e)) || [];
    return message;
  },
};

messageTypeRegistry.set(Config.$type, Config);

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

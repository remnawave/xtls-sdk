// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.4
// source: transport/internet/finalmask/sudoku/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../typeRegistry";

export const protobufPackage = "xray.transport.internet.finalmask.sudoku";

export interface Config {
  $type: "xray.transport.internet.finalmask.sudoku.Config";
  password: string;
  ascii: string;
  customTable: string;
  paddingMin: number;
  paddingMax: number;
  customTables: string[];
}

function createBaseConfig(): Config {
  return {
    $type: "xray.transport.internet.finalmask.sudoku.Config",
    password: "",
    ascii: "",
    customTable: "",
    paddingMin: 0,
    paddingMax: 0,
    customTables: [],
  };
}

export const Config: MessageFns<Config, "xray.transport.internet.finalmask.sudoku.Config"> = {
  $type: "xray.transport.internet.finalmask.sudoku.Config" as const,

  encode(message: Config, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.password !== "") {
      writer.uint32(10).string(message.password);
    }
    if (message.ascii !== "") {
      writer.uint32(18).string(message.ascii);
    }
    if (message.customTable !== "") {
      writer.uint32(26).string(message.customTable);
    }
    if (message.paddingMin !== 0) {
      writer.uint32(32).uint32(message.paddingMin);
    }
    if (message.paddingMax !== 0) {
      writer.uint32(40).uint32(message.paddingMax);
    }
    for (const v of message.customTables) {
      writer.uint32(58).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Config {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ascii = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.customTable = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.paddingMin = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.paddingMax = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.customTables.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Config {
    return {
      $type: Config.$type,
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      ascii: isSet(object.ascii) ? globalThis.String(object.ascii) : "",
      customTable: isSet(object.customTable)
        ? globalThis.String(object.customTable)
        : isSet(object.custom_table)
        ? globalThis.String(object.custom_table)
        : "",
      paddingMin: isSet(object.paddingMin)
        ? globalThis.Number(object.paddingMin)
        : isSet(object.padding_min)
        ? globalThis.Number(object.padding_min)
        : 0,
      paddingMax: isSet(object.paddingMax)
        ? globalThis.Number(object.paddingMax)
        : isSet(object.padding_max)
        ? globalThis.Number(object.padding_max)
        : 0,
      customTables: globalThis.Array.isArray(object?.customTables)
        ? object.customTables.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.custom_tables)
        ? object.custom_tables.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: Config): unknown {
    const obj: any = {};
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.ascii !== "") {
      obj.ascii = message.ascii;
    }
    if (message.customTable !== "") {
      obj.customTable = message.customTable;
    }
    if (message.paddingMin !== 0) {
      obj.paddingMin = Math.round(message.paddingMin);
    }
    if (message.paddingMax !== 0) {
      obj.paddingMax = Math.round(message.paddingMax);
    }
    if (message.customTables?.length) {
      obj.customTables = message.customTables;
    }
    return obj;
  },

  create(base?: DeepPartial<Config>): Config {
    return Config.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Config>): Config {
    const message = createBaseConfig();
    message.password = object.password ?? "";
    message.ascii = object.ascii ?? "";
    message.customTable = object.customTable ?? "";
    message.paddingMin = object.paddingMin ?? 0;
    message.paddingMax = object.paddingMax ?? 0;
    message.customTables = object.customTables?.map((e) => e) || [];
    return message;
  },
};

messageTypeRegistry.set(Config.$type, Config);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

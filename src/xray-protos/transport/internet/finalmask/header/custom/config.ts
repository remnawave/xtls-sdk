// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.4
// source: transport/internet/finalmask/header/custom/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";

export const protobufPackage = "xray.transport.internet.finalmask.header.custom";

export interface TCPItem {
  $type: "xray.transport.internet.finalmask.header.custom.TCPItem";
  delayMin: number;
  delayMax: number;
  rand: number;
  randMin: number;
  randMax: number;
  packet: Uint8Array;
}

export interface TCPSequence {
  $type: "xray.transport.internet.finalmask.header.custom.TCPSequence";
  sequence: TCPItem[];
}

export interface TCPConfig {
  $type: "xray.transport.internet.finalmask.header.custom.TCPConfig";
  clients: TCPSequence[];
  servers: TCPSequence[];
  errors: TCPSequence[];
}

export interface UDPItem {
  $type: "xray.transport.internet.finalmask.header.custom.UDPItem";
  rand: number;
  randMin: number;
  randMax: number;
  packet: Uint8Array;
}

export interface UDPConfig {
  $type: "xray.transport.internet.finalmask.header.custom.UDPConfig";
  client: UDPItem[];
  server: UDPItem[];
}

function createBaseTCPItem(): TCPItem {
  return {
    $type: "xray.transport.internet.finalmask.header.custom.TCPItem",
    delayMin: 0,
    delayMax: 0,
    rand: 0,
    randMin: 0,
    randMax: 0,
    packet: new Uint8Array(0),
  };
}

export const TCPItem: MessageFns<TCPItem, "xray.transport.internet.finalmask.header.custom.TCPItem"> = {
  $type: "xray.transport.internet.finalmask.header.custom.TCPItem" as const,

  encode(message: TCPItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.delayMin !== 0) {
      writer.uint32(8).int64(message.delayMin);
    }
    if (message.delayMax !== 0) {
      writer.uint32(16).int64(message.delayMax);
    }
    if (message.rand !== 0) {
      writer.uint32(24).int32(message.rand);
    }
    if (message.randMin !== 0) {
      writer.uint32(32).int32(message.randMin);
    }
    if (message.randMax !== 0) {
      writer.uint32(40).int32(message.randMax);
    }
    if (message.packet.length !== 0) {
      writer.uint32(50).bytes(message.packet);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TCPItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTCPItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.delayMin = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.delayMax = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rand = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.randMin = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.randMax = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.packet = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TCPItem {
    return {
      $type: TCPItem.$type,
      delayMin: isSet(object.delayMin)
        ? globalThis.Number(object.delayMin)
        : isSet(object.delay_min)
        ? globalThis.Number(object.delay_min)
        : 0,
      delayMax: isSet(object.delayMax)
        ? globalThis.Number(object.delayMax)
        : isSet(object.delay_max)
        ? globalThis.Number(object.delay_max)
        : 0,
      rand: isSet(object.rand) ? globalThis.Number(object.rand) : 0,
      randMin: isSet(object.randMin)
        ? globalThis.Number(object.randMin)
        : isSet(object.rand_min)
        ? globalThis.Number(object.rand_min)
        : 0,
      randMax: isSet(object.randMax)
        ? globalThis.Number(object.randMax)
        : isSet(object.rand_max)
        ? globalThis.Number(object.rand_max)
        : 0,
      packet: isSet(object.packet) ? bytesFromBase64(object.packet) : new Uint8Array(0),
    };
  },

  toJSON(message: TCPItem): unknown {
    const obj: any = {};
    if (message.delayMin !== 0) {
      obj.delayMin = Math.round(message.delayMin);
    }
    if (message.delayMax !== 0) {
      obj.delayMax = Math.round(message.delayMax);
    }
    if (message.rand !== 0) {
      obj.rand = Math.round(message.rand);
    }
    if (message.randMin !== 0) {
      obj.randMin = Math.round(message.randMin);
    }
    if (message.randMax !== 0) {
      obj.randMax = Math.round(message.randMax);
    }
    if (message.packet.length !== 0) {
      obj.packet = base64FromBytes(message.packet);
    }
    return obj;
  },

  create(base?: DeepPartial<TCPItem>): TCPItem {
    return TCPItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TCPItem>): TCPItem {
    const message = createBaseTCPItem();
    message.delayMin = object.delayMin ?? 0;
    message.delayMax = object.delayMax ?? 0;
    message.rand = object.rand ?? 0;
    message.randMin = object.randMin ?? 0;
    message.randMax = object.randMax ?? 0;
    message.packet = object.packet ?? new Uint8Array(0);
    return message;
  },
};

messageTypeRegistry.set(TCPItem.$type, TCPItem);

function createBaseTCPSequence(): TCPSequence {
  return { $type: "xray.transport.internet.finalmask.header.custom.TCPSequence", sequence: [] };
}

export const TCPSequence: MessageFns<TCPSequence, "xray.transport.internet.finalmask.header.custom.TCPSequence"> = {
  $type: "xray.transport.internet.finalmask.header.custom.TCPSequence" as const,

  encode(message: TCPSequence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.sequence) {
      TCPItem.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TCPSequence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTCPSequence();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sequence.push(TCPItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TCPSequence {
    return {
      $type: TCPSequence.$type,
      sequence: globalThis.Array.isArray(object?.sequence) ? object.sequence.map((e: any) => TCPItem.fromJSON(e)) : [],
    };
  },

  toJSON(message: TCPSequence): unknown {
    const obj: any = {};
    if (message.sequence?.length) {
      obj.sequence = message.sequence.map((e) => TCPItem.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TCPSequence>): TCPSequence {
    return TCPSequence.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TCPSequence>): TCPSequence {
    const message = createBaseTCPSequence();
    message.sequence = object.sequence?.map((e) => TCPItem.fromPartial(e)) || [];
    return message;
  },
};

messageTypeRegistry.set(TCPSequence.$type, TCPSequence);

function createBaseTCPConfig(): TCPConfig {
  return { $type: "xray.transport.internet.finalmask.header.custom.TCPConfig", clients: [], servers: [], errors: [] };
}

export const TCPConfig: MessageFns<TCPConfig, "xray.transport.internet.finalmask.header.custom.TCPConfig"> = {
  $type: "xray.transport.internet.finalmask.header.custom.TCPConfig" as const,

  encode(message: TCPConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.clients) {
      TCPSequence.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.servers) {
      TCPSequence.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.errors) {
      TCPSequence.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TCPConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTCPConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clients.push(TCPSequence.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.servers.push(TCPSequence.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errors.push(TCPSequence.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TCPConfig {
    return {
      $type: TCPConfig.$type,
      clients: globalThis.Array.isArray(object?.clients) ? object.clients.map((e: any) => TCPSequence.fromJSON(e)) : [],
      servers: globalThis.Array.isArray(object?.servers) ? object.servers.map((e: any) => TCPSequence.fromJSON(e)) : [],
      errors: globalThis.Array.isArray(object?.errors) ? object.errors.map((e: any) => TCPSequence.fromJSON(e)) : [],
    };
  },

  toJSON(message: TCPConfig): unknown {
    const obj: any = {};
    if (message.clients?.length) {
      obj.clients = message.clients.map((e) => TCPSequence.toJSON(e));
    }
    if (message.servers?.length) {
      obj.servers = message.servers.map((e) => TCPSequence.toJSON(e));
    }
    if (message.errors?.length) {
      obj.errors = message.errors.map((e) => TCPSequence.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TCPConfig>): TCPConfig {
    return TCPConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TCPConfig>): TCPConfig {
    const message = createBaseTCPConfig();
    message.clients = object.clients?.map((e) => TCPSequence.fromPartial(e)) || [];
    message.servers = object.servers?.map((e) => TCPSequence.fromPartial(e)) || [];
    message.errors = object.errors?.map((e) => TCPSequence.fromPartial(e)) || [];
    return message;
  },
};

messageTypeRegistry.set(TCPConfig.$type, TCPConfig);

function createBaseUDPItem(): UDPItem {
  return {
    $type: "xray.transport.internet.finalmask.header.custom.UDPItem",
    rand: 0,
    randMin: 0,
    randMax: 0,
    packet: new Uint8Array(0),
  };
}

export const UDPItem: MessageFns<UDPItem, "xray.transport.internet.finalmask.header.custom.UDPItem"> = {
  $type: "xray.transport.internet.finalmask.header.custom.UDPItem" as const,

  encode(message: UDPItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rand !== 0) {
      writer.uint32(8).int32(message.rand);
    }
    if (message.randMin !== 0) {
      writer.uint32(16).int32(message.randMin);
    }
    if (message.randMax !== 0) {
      writer.uint32(24).int32(message.randMax);
    }
    if (message.packet.length !== 0) {
      writer.uint32(34).bytes(message.packet);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UDPItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUDPItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rand = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.randMin = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.randMax = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.packet = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UDPItem {
    return {
      $type: UDPItem.$type,
      rand: isSet(object.rand) ? globalThis.Number(object.rand) : 0,
      randMin: isSet(object.randMin)
        ? globalThis.Number(object.randMin)
        : isSet(object.rand_min)
        ? globalThis.Number(object.rand_min)
        : 0,
      randMax: isSet(object.randMax)
        ? globalThis.Number(object.randMax)
        : isSet(object.rand_max)
        ? globalThis.Number(object.rand_max)
        : 0,
      packet: isSet(object.packet) ? bytesFromBase64(object.packet) : new Uint8Array(0),
    };
  },

  toJSON(message: UDPItem): unknown {
    const obj: any = {};
    if (message.rand !== 0) {
      obj.rand = Math.round(message.rand);
    }
    if (message.randMin !== 0) {
      obj.randMin = Math.round(message.randMin);
    }
    if (message.randMax !== 0) {
      obj.randMax = Math.round(message.randMax);
    }
    if (message.packet.length !== 0) {
      obj.packet = base64FromBytes(message.packet);
    }
    return obj;
  },

  create(base?: DeepPartial<UDPItem>): UDPItem {
    return UDPItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UDPItem>): UDPItem {
    const message = createBaseUDPItem();
    message.rand = object.rand ?? 0;
    message.randMin = object.randMin ?? 0;
    message.randMax = object.randMax ?? 0;
    message.packet = object.packet ?? new Uint8Array(0);
    return message;
  },
};

messageTypeRegistry.set(UDPItem.$type, UDPItem);

function createBaseUDPConfig(): UDPConfig {
  return { $type: "xray.transport.internet.finalmask.header.custom.UDPConfig", client: [], server: [] };
}

export const UDPConfig: MessageFns<UDPConfig, "xray.transport.internet.finalmask.header.custom.UDPConfig"> = {
  $type: "xray.transport.internet.finalmask.header.custom.UDPConfig" as const,

  encode(message: UDPConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.client) {
      UDPItem.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.server) {
      UDPItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UDPConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUDPConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.client.push(UDPItem.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.server.push(UDPItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UDPConfig {
    return {
      $type: UDPConfig.$type,
      client: globalThis.Array.isArray(object?.client) ? object.client.map((e: any) => UDPItem.fromJSON(e)) : [],
      server: globalThis.Array.isArray(object?.server) ? object.server.map((e: any) => UDPItem.fromJSON(e)) : [],
    };
  },

  toJSON(message: UDPConfig): unknown {
    const obj: any = {};
    if (message.client?.length) {
      obj.client = message.client.map((e) => UDPItem.toJSON(e));
    }
    if (message.server?.length) {
      obj.server = message.server.map((e) => UDPItem.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<UDPConfig>): UDPConfig {
    return UDPConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UDPConfig>): UDPConfig {
    const message = createBaseUDPConfig();
    message.client = object.client?.map((e) => UDPItem.fromPartial(e)) || [];
    message.server = object.server?.map((e) => UDPItem.fromPartial(e)) || [];
    return message;
  },
};

messageTypeRegistry.set(UDPConfig.$type, UDPConfig);

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
